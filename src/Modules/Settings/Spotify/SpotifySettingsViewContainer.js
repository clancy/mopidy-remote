import {connect} from 'react-redux';
import SpotifySettingsView from './SpotifySettingsView';

const mapStateToProps = (state) => {
  return {
  }
}

export default connect(mapStateToProps)(SpotifySettingsView);
